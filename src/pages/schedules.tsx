import { SchedulesPage } from '../components/schedules/SchedulesPage';
import { Layout } from '../common/layout/Layout';

export default () => (
    <Layout>
        <SchedulesPage />
    </Layout>
);
