import { LocationsPage } from '../components/locations/LocationsPage';
import { Layout } from '../common/layout/Layout';

export default () => (
    <Layout>
        <LocationsPage />
    </Layout>
);
