import { DocumentsPage } from '../components/documents/DocumentsPage';
import { Layout } from '../common/layout/Layout';

export default () => (
    <Layout>
        <DocumentsPage />
    </Layout>
);
