import React, {FunctionComponent} from 'react';
import {GappedStack} from '../common';

export const MoreInfoPage: FunctionComponent = () => {
    return (
        <GappedStack padded>

        </GappedStack>
    )
}
