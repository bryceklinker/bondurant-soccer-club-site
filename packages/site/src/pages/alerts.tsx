import { Layout } from '../common/layout/Layout';
import { AlertsPage } from '../components/alerts/AlertsPage';

export default () => {
    return (
        <Layout>
            <AlertsPage />
        </Layout>
    );
};
