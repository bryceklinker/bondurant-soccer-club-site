{
  "name": "backend",
  "version": "1.0.0",
  "scripts": {
    "build": "dotnet build",
    "test": "dotnet test",
    "clean": "dotnet clean",
    "serve": "cd src/Bsc.Function && func start"
  },
  "dependencies": {
  }
}
