{
  "name": "backend",
  "version": "1.0.0",
  "scripts": {
    "build": "dotnet build --configuration Release --output ../../dist/backend",
    "test": "dotnet test",
    "clean": "dotnet clean",
    "serve": "echo 'skipping serve...'"
  },
  "dependencies": {
  }
}
